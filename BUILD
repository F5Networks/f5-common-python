Install the requirements for distutils bdist_deb and bdist_rpm to build Debian or RHEL packages respectively.

To build Debian packages:

python setup.py --command-packages=stdeb.command bdist_deb;

To build RHEL packages:

python setup.py bdist_rpm;


